% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imr_fit.R
\name{imr_fit}
\alias{imr_fit}
\title{Fit an IMR Model}
\usage{
imr_fit(
  data,
  rank = 2,
  lambda_m = 0,
  lambda_beta = 0,
  lambda_gamma = 0,
  convergence = imr_convergence(),
  warm_start = NULL,
  training = FALSE
)
}
\arguments{
\item{data}{An object of class \code{"imr_data"}. This object contains the response
matrix, optional covariates and similarities, and the specific model structure to be fitted.}

\item{rank}{Integer. The rank of the low-rank component (\eqn{M=U D V^T}). This is
required if the model structure includes a low-rank component. Defaults to \code{2}.}

\item{lambda_m}{Numeric. The regularization parameter for the low-rank component.
Required if the low-rank component is included in the model. Defaults to \code{0}.}

\item{lambda_beta}{Numeric. The regularization parameter for the row covariates.
Only used if row covariates are specified in the \code{imr_data} model structure.
Defaults to \code{0}.}

\item{lambda_gamma}{Numeric. The regularization parameter for the column covariates.
Only used if column covariates are specified in the \code{imr_data} model structure.
Defaults to \code{0}.}

\item{convergence}{An object of class \code{"imr_convergence"}, generated by
\code{imr_convergence()}. It dictates the maximum iterations, threshold, tracing,
and initialization method.}

\item{warm_start}{An optional object of class \code{"imr_fit"}. If provided, the
coefficients from this previous fit are used as the starting values for the
new optimization, overriding the initialization strategy in \code{convergence}.
Defaults to \code{NULL}.}

\item{training}{Logical. If \code{TRUE}, the model fits on \code{data$y_train} rather than
the full \code{data$Y} matrix. This is primarily intended for internal use during
cross-validation. Generally, users should leave this as \code{FALSE}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"imr_fit"}. This is a list containing:
\itemize{
\item \code{coefficients}: A list of the estimated model parameters, which may include
the low-rank matrices (\code{u}, \code{d}, \code{v}), covariate effects
(\code{beta}, \code{gamma}), and intercepts (\code{beta0}, \code{gamma0}).
\item \code{residuals}: A sparse matrix of the fitted residuals.
\item \code{Xr}, \code{Zr}: The pxp and qxq R matrices generated from the QR-decomposition
of the row and column covariates. They are copied from \code{data} and
used to transform the estimated coefficients to
the original scale. (used for the \code{summary} method)
\item \code{meta}: A list of fitting metadata, including the iteration count,
convergence status, and sum of squares components (used for the \code{print} method).
\item \code{convergence}: The convergence control parameters used.
\item \code{model}: The logical model structure inherited from \code{data}.
\item \code{meta_data}: Variable metadata inherited from \code{data}.
}
}
\description{
The main function for fitting an Incomplete Matrix Regression (IMR) model.
It takes a prepared \code{imr_data} object and estimates the parameters based on the
specified model structure and regularization parameters.
}
\examples{
# create sample data
Y <- matrix(
  c(2, NA, 3,
  4, .5, NA,
  NA, NA, 5), 3, byrow= TRUE
)

# create covariate matrix of 2 variables
X <- matrix(rnorm(3*2), 3, 2)

# create the data object
data <- imr_data(Y =  Y, X = X, val_prop = 0.0)

# update the model to add row intercepts
data <- update(data, row_intercept = TRUE)

# fit the model
fit <- imr_fit(data, rank = 2)

# print the model fit summary
print(fit)

# print a summary of the fitted coefficients
summary(fit)

# extract the coefficients
coefs <- coef(fit)

# estimate the target matrix
target <- reconstruct(fit, data)$estimates

# compute estimates of the training data
estimates <- reconstruct_partial(fit, data, data$Y@i, data$Y@p, return_matrix = FALSE)

# compute the training Root Mean Squared Error
evaluate(estimates, data$Y@x, metric = "RMSE")

}
\references{
Fouda, K., Labbe, A., & Oualkacha, K. (2026).
\emph{Incomplete Matrix Regression}.
\url{https://arxiv.org/abs/2606.26325}
}
\seealso{
\code{\link[=imr_data]{imr_data()}}, \code{\link[=imr_convergence]{imr_convergence()}}, \code{\link[=print.imr_fit]{print.imr_fit()}}, \code{\link[=summary.imr_fit]{summary.imr_fit()}}, \code{\link[=coef.imr_fit]{coef.imr_fit()}}
}
