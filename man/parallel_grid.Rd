% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_wrapper.R
\name{parallel_grid}
\alias{parallel_grid}
\title{Run a 2-parameter grid in parallel}
\usage{
parallel_grid(
  grid,
  f,
  combine = "list",
  .seed = TRUE,
  .packages = NULL,
  .progress = FALSE,
  ...
)
}
\arguments{
\item{grid}{Named list of length 2 with elements \code{lambda_beta} and
\code{lambda_gamma}. Each element should be an atomic vector of candidate values.}

\item{f}{Function to be evaluated on each parameter combination. It must
accept arguments named \code{lambda_beta} and \code{lambda_gamma}, along with any
additional arguments passed through \code{...}.}

\item{combine}{How to combine the individual results. \code{"list"} (default)
returns a list; \code{"rbind"} row-binds data frame results and adds the
tuning-parameter columns.}

\item{.seed}{Value passed to \code{future.apply::future_lapply()} as
\code{future.seed}. Set to \code{TRUE} for reproducible parallel RNG streams.}

\item{.packages}{Character vector of package names to attach on workers;
passed to \code{future.apply::future_lapply()} as \code{future.packages}.}

\item{.progress}{Logical. If \code{TRUE} and the {progressr} package is installed,
a progress bar is shown while evaluating the grid.}

\item{...}{Additional arguments forwarded to \code{f}.}
}
\value{
A list or data frame, depending on \code{combine}:
\itemize{
\item If \code{combine = "list"}, a list of results, with an attached \code{"grid"}
attribute containing the parameter combinations.
\item If \code{combine = "rbind"}, a single data frame created by row-binding all
results and adding the tuning-parameter columns.
}
}
\description{
Evaluate a function over all combinations of two tuning parameters
(\code{lambda_beta} and \code{lambda_gamma}) using the future ecosystem for parallel
execution.
}
\details{
This helper is currently specialised to grids over \code{lambda_beta} and
\code{lambda_gamma}.

The function constructs the full Cartesian product of the supplied
\code{lambda_beta} and \code{lambda_gamma} values, evaluates \code{f()} for each
combination in parallel via \code{future.apply::future_lapply()}, and then
combines the results.

When \code{combine = "list"}, the result is a list of length equal to the number
of parameter combinations. The original grid of combinations is attached as
the \code{"grid"} attribute.

When \code{combine = "rbind"}, every call to \code{f()} must return a data frame. Two
extra columns corresponding to the grid parameters are added to each result
before row-binding into a single data frame.

The function requires the {future.apply} package, and optionally {progressr}
when \code{.progress = TRUE}.
}
\examples{
\dontrun{
grid <- list(
  lambda_beta  = c(0.1, 1),
  lambda_gamma = c(0, 0.5)
)

f <- function(lambda_beta, lambda_gamma) {
  data.frame(
    lambda_beta  = lambda_beta,
    lambda_gamma = lambda_gamma,
    score        = lambda_beta + lambda_gamma
  )
}

# Return a data frame with parameter columns attached
parallel_grid(grid, f, combine = "rbind")
}

}
