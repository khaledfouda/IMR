% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imr_data.R
\name{update.imr_data}
\alias{update.imr_data}
\title{Update IMR Model Structure}
\usage{
\method{update}{imr_data}(
  object,
  row_covariates = NULL,
  col_covariates = NULL,
  low_rank_component = NULL,
  row_similarity = NULL,
  col_similarity = NULL,
  row_intercept = NULL,
  col_intercept = NULL,
  shared_beta = NULL,
  shared_gamma = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"imr_data"}.}

\item{row_covariates}{Logical. Include row covariates in the model?
(Requires \code{X} to have been provided to \code{imr_data}).}

\item{col_covariates}{Logical. Include column covariates in the model?
(Requires \code{Z} to have been provided to \code{imr_data}).}

\item{low_rank_component}{Logical. Include the low-rank component (\eqn{M=U D V^T})?}

\item{row_similarity}{Logical. Include row similarity penalties?
(Requires \code{similarity_rows} to have been provided to \code{imr_data}).}

\item{col_similarity}{Logical. Include column similarity penalties?
(Requires \code{similarity_cols} to have been provided to \code{imr_data}).}

\item{row_intercept}{Logical. Include a row intercept/bias term?}

\item{col_intercept}{Logical. Include a column intercept/bias term?}

\item{shared_beta}{Logical. Should row covariate effects (\code{beta}) be shared
across all columns?}

\item{shared_gamma}{Logical. Should column covariate effects (\code{gamma}) be shared
across all rows?}

\item{...}{Additional arguments (ignored).}
}
\value{
The modified \code{"imr_data"} object with updated \verb{$model} flags.
}
\description{
Updates the logical model structure of an existing \code{"imr_data"} object. This allows
you to toggle model components (like intercepts, covariates, or low-rank components)
on or off without needing to re-process the underlying data matrices.
}
\examples{

# create sample data
Y <- matrix(
  c(2, NA, 3,
  4, .5, NA,
  NA, NA, 5), 3, byrow= TRUE
)

# create covariate matrix of 2 variables
X <- matrix(rnorm(3*2), 3, 2)

# create the data object
data <- imr_data(Y =  Y, X = X, val_prop = 0.0)

# update the model to add row intercepts
data <- update(data, row_intercept = TRUE)

# print the metadata
print(data)

}
\seealso{
\code{\link[=imr_data]{imr_data()}}, \code{\link[=imr_similarity]{imr_similarity()}}
}
