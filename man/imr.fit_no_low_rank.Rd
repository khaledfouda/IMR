% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_function.R
\name{imr.fit_no_low_rank}
\alias{imr.fit_no_low_rank}
\title{Fit Incomplete Matrix Regression (IMR) Model without the low-rank \eqn{M}}
\usage{
imr.fit_no_low_rank(
  Y,
  X = NULL,
  Z = NULL,
  lambda_beta = NULL,
  lambda_gamma = NULL,
  intercept_row = FALSE,
  intercept_col = FALSE,
  maxit = 300,
  thresh = 1e-05,
  warm_start = NULL,
  trace = FALSE
)
}
\arguments{
\item{Y}{An incomplete matrix (class \code{Incomplete}; see \code{\link{as.incomplete}}).
The target matrix to be completed (n by m).}

\item{X}{Optional matrix of row covariates (n by p). Default is \code{NULL}.}

\item{Z}{Optional matrix of column covariates (m by q). Default is \code{NULL}.}

\item{lambda_beta}{Numeric scalar. Controls the Lasso penalty on the row
covariates. Default is 0.}

\item{lambda_gamma}{Numeric scalar. Controls the Lasso penalty on the column
covariates. Default is 0.}

\item{intercept_row}{Logical. Include row-level intercepts? Default is \code{FALSE}.}

\item{intercept_col}{Logical. Include column-level intercepts? Default is \code{FALSE}.}

\item{maxit}{Integer. Maximum number of iterations. Default is 300.}

\item{thresh}{Numeric scalar. Convergence threshold based on the Frobenius
difference between updates. Default is 1e-5.}

\item{warm_start}{Optional list. A previous result object from \code{imr.fit}
to use as initial values. Default is \code{NULL}.}

\item{trace}{Logical. If \code{TRUE}, prints the objective function value at
each step. Default is \code{FALSE}.}
}
\value{
A list containing the learned parameters:
\describe{
\item{resid}{An incomplete matrix of the last iteration's residuals (i.e., the model's training errors)}
\item{beta}{Matrix of row covariate coefficients. \code{NULL} if \code{X} is \code{NULL}.}
\item{gamma}{Matrix of column covariate coefficients. \code{NULL} if \code{Z} is \code{NULL}.}
\item{beta0}{Vector of row-level intercepts. \code{NULL} if \code{intercept_row} is \code{FALSE}.}
\item{gamma0}{Vector of column-level intercepts. \code{NULL} if \code{intercept_col} is \code{FALSE}.}
\item{n_iter}{Integer. The number of iterations performed.}
}
}
\description{
\code{imr.fit_no_low_rank} is similar to \code{imr.fit} except that it does not fit the low-rank matrix structure.
}
