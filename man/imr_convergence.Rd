% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imr_fit.R
\name{imr_convergence}
\alias{imr_convergence}
\title{Control Parameters for IMR Convergence}
\usage{
imr_convergence(
  maxit = 600,
  thresh = 1e-05,
  trace = FALSE,
  ls_initial = TRUE,
  huber_max_sample = 1e+05
)
}
\arguments{
\item{maxit}{Integer. The maximum number of iterations allowed for the fit
function. Defaults to \code{600}.}

\item{thresh}{Numeric. The convergence threshold. Training stops early if
the Frobenius difference between the new and old parameters falls below
this value. Defaults to \code{1e-5}.}

\item{trace}{Logical. If \code{TRUE}, prints the Frobenius ratio and the loss
function value at each iteration. Defaults to \code{FALSE}.}

\item{ls_initial}{Logical. If \code{TRUE} (the default), least-squares initial
values are used. If \code{FALSE}, random initialization is used. It is
recommended to leave this as \code{TRUE} to speed up convergence.}
}
\value{
An object of class \code{"imr_convergence"}, which is a list containing
the specified control parameters.
}
\description{
Defines and creates a structure for the convergence parameters used by the
IMR fit function.
}
\examples{
# Use the default convergence parameters
convergence <- imr_convergence()
print(convergence)
}
\seealso{
\code{\link[=imr_fit]{imr_fit()}}, \code{\link[=print.imr_convergence]{print.imr_convergence()}}
}
