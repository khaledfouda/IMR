% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{svd_opt}
\alias{svd_opt}
\title{Optimized Singular Value Decomposition}
\usage{
svd_opt(mat, k = NULL, tol = NULL)
}
\arguments{
\item{mat}{A numeric matrix. Can be a base R dense matrix or a Sparse matrix.}

\item{k}{Integer. The number of singular values and corresponding eigenvectors
to compute or retain. If \code{NULL} or greater than or equal to the maximum
possible rank of the matrix, a full SVD is computed.}

\item{tol}{Numeric. A tolerance threshold for eigenvalue truncation. After
computation, any singular values less than or equal to \code{tol} (and their
corresponding eigenvectors in \code{u} and \code{v}) are removed. Defaults to \code{NULL}
(no threshold applied).}
}
\value{
A list containing the SVD components:
\itemize{
\item \code{d}: A vector containing the computed singular values.
\item \code{u}: A matrix whose columns contain the left singular vectors.
\item \code{v}: A matrix whose columns contain the right singular vectors.
}
}
\description{
A general-purpose wrapper for Singular Value Decomposition (SVD) that
selects the most computationally efficient backend based on the matrix
dimensions, sparsity, and the desired number of singular components.
}
\details{
To minimize computation time, \code{svd_opt} routes the SVD request to different
algorithms depending on the scenario:

\itemize{
\item {Thin Matrices:} If the matrix is wide
(\code{ncol > 2 * nrow}) or tall (\code{nrow > 2 * ncol}), it utilizes
internal C++ functions (\code{svd_small_nr_cpp} or
\code{svd_small_nc_cpp}).
\item {Full SVD:} If \code{k} is \code{NULL} or requests the full rank, it uses
base R's \code{\link[base]{svd}} function.
\item {Partial SVD (Sparse or \code{k <= 5}):} For large matrices where
only a few components are needed or if the matrix is sparse, it
uses the \code{\link[irlba]{irlba}}.
\item {Partial SVD (Dense and \code{k >= 5}):} For dense matrices where
a larger number of components are requested, it uses the
\code{\link[RSpectra]{svds}}).
}
}
\examples{
x <- matrix(rnorm(100),10, 10)
# return the first singular value and vectors
svd_opt(x, k = 1)

}
