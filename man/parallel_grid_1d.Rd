% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_wrapper.R
\name{parallel_grid_1d}
\alias{parallel_grid_1d}
\title{Parallel search over a 1D parameter grid}
\usage{
parallel_grid_1d(
  grid,
  f,
  .seed = TRUE,
  .packages = NULL,
  .progress = FALSE,
  ...
)
}
\arguments{
\item{grid}{Atomic vector of candidate values for the tuning parameter.}

\item{f}{Function to be evaluated on each parameter value. It must have the
signature \code{f(parameter, ...)} and return a list with components:
\describe{
\item{error}{Numeric scalar giving the validation error for that fit.}
\item{fit}{An arbitrary R object (typically a model fit).}
}}

\item{.seed}{Value passed to \code{future.apply::future_lapply()} as
\code{future.seed}. Set to \code{TRUE} for reproducible parallel RNG streams.}

\item{.packages}{Character vector of package names to attach on workers;
passed to \code{future.apply::future_lapply()} as \code{future.packages}.}

\item{.progress}{Logical. If \code{TRUE} and the {progressr} package is installed,
a progress bar is shown while evaluating the grid.}

\item{...}{Additional arguments forwarded to \code{f}.}
}
\value{
A list with components:

\describe{
\item{results}{A data frame with columns \code{parameter} and \code{error}, giving
the grid and corresponding validation errors.}
\item{best_parameter}{The parameter value with the smallest error.}
\item{best_error}{The smallest error value.}
\item{best_fit}{The \code{fit} object corresponding to \code{best_parameter}.}
}
}
\description{
Evaluate a function over a one-dimensional grid of parameter values using the
future ecosystem for parallel execution. The function \code{f} is expected to take
a single argument \code{parameter} (the grid value) plus any additional arguments
passed through \code{...}, and return a list with elements \code{error} and \code{fit}.
}
\details{
For each value in \code{grid}, the function calls:

\preformatted{
  f(parameter = grid[i], ...)
}

and expects a list with elements named \code{error} and \code{fit}. The \code{error}
values are collected into a data frame alongside the corresponding
parameter values, and the parameter with the smallest error is selected as
the best.

Parallelisation is handled via \code{future.apply::future_lapply()}. A progress
bar is optionally shown using {progressr} when \code{.progress = TRUE}.
}
\examples{
\dontrun{
grid <- seq(0, 1, length.out = 5)

f <- function(parameter, n = 100) {
  # Dummy example: quadratic loss around 0.4
  err <- (parameter - 0.4)^2 + rnorm(1, sd = 0.01)
  fit <- list(param = parameter, n = n)
  list(error = err, fit = fit)
}

res <- parallel_grid_1d(
  grid = grid,
  f = f,
  .progress = FALSE
)

res$results
res$best_parameter
res$best_error
str(res$best_fit)
}

}
