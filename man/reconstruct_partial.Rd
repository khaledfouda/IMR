% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imr_data.R
\name{reconstruct_partial}
\alias{reconstruct_partial}
\title{Partially Reconstruct Model Estimates for Specific Indices}
\usage{
reconstruct_partial(
  fit,
  data,
  irow,
  pcol,
  trace = FALSE,
  return_matrix = FALSE
)
}
\arguments{
\item{fit}{An object of class \code{"imr_fit"}, generated by \code{\link{imr_fit}}.}

\item{data}{An object of class \code{"imr_data"}generated by \code{\link{imr_data}}.}

\item{irow, pcol}{Objects of class \verb{"integer} corresponding to the \verb{@i} and \verb{@p}
slots in a \verb{"CsparseMatrix} denoting the indices of the elements to be estimated.
The returned object corresponds to the \code{x} slot of said object.}

\item{trace}{Logical. If \code{TRUE}, prints progress messages for each reconstruction
step. Defaults to \code{FALSE}.}

\item{return_matrix}{Logical. If \code{TRUE} then it returns a \code{CsparseMatrix} with
the following slots: \verb{@i=irow}, \verb{@p=pcol}, \verb{@x=estimates}. If \code{FALSE}, only
the \verb{@x} slot is returned as a vector. Defaults to \code{FALSE}.}
}
\value{
Either a \code{CsparseMatrix} object or a numeric vector (see above).
}
\description{
Computes the estimated response matrix from a fitted IMR model, but \emph{only} for
a specific subset of matrix entries. This is highly memory-efficient and fast
when you only need to evaluate predictions on a test set or validation mask,
as it avoids computing the full dense prediction matrix.
}
\details{
Unlike the full \code{\link{reconstruct}} function, which allocates and calculates
every cell of an \eqn{n \times m} matrix, \code{reconstruct_partial} relies on optimized C++
routines to calculate only the targeted entries.
}
