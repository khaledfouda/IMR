% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imr_data.R
\name{reconstruct_partial}
\alias{reconstruct_partial}
\title{Partially Reconstruct Model Estimates for Specific Indices}
\usage{
reconstruct_partial(
  fit,
  data,
  irow,
  pcol,
  trace = FALSE,
  return_matrix = FALSE
)
}
\arguments{
\item{fit}{An object of class \code{"imr_fit"}, typically generated by \code{\link{imr_fit}}.}

\item{data}{An object of class \code{"imr_data"} containing the covariates used during
fitting.}

\item{trace}{Logical. If \code{TRUE}, prints progress messages for each reconstruction
step. Defaults to \code{FALSE}.}

\item{target}{An \code{Incomplete} (sparse) matrix. The function uses the structural
row and column indices (\verb{@i} and \verb{@p}) of this matrix to determine exactly
where to compute predictions. The actual numerical values inside this matrix
do not matter, as they will be overwritten.}
}
\value{
The original \code{target} sparse matrix, but with its values (\verb{@x} slot)
replaced by the model's reconstructed predictions for those coordinates.
}
\description{
Computes the estimated response matrix from a fitted IMR model, but \emph{only} for
a specific subset of matrix entries. This is highly memory-efficient and fast
when you only need to evaluate predictions on a test set or validation mask,
as it avoids computing the full dense prediction matrix.
}
\details{
Unlike the full \code{\link{reconstruct}} function, which allocates and calculates
every cell of an $n \times m$ matrix, \code{reconstruct_partial} relies on optimized C++
routines to calculate only the targeted entries.
}
